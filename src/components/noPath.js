import React, { useState } from 'react';

export default function NoPath(props){
   return(
     <div>
       There is no path to this page, return to home site and navigate from there!
     </div>
   );
}