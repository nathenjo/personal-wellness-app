import React, { useState } from 'react';

export default function NoPath(props){
   return(
     <div className='no-path'>
      <h2>This page cannot be found or does not exist,</h2>
      <h2>return to home page and navigate from there!</h2>
     </div>
   );
}